# Exact pins across the board: the @miragon-ai packages release in lockstep
# (upgrade them together to one version) and @miragon/mcp-toolkit-* peers
# mcp-use exactly — a floating range that drifts to a second toolkit instance
# resurrects the duplicate-React-context bug (widgets hang on "Loading…").
save-exact=true
shell-emulator=true
