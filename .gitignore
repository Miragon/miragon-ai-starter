node_modules/
dist/
coverage/
*.tsbuildinfo
# mcp-use dev artifacts (tool-registry types)
.mcp-use/
# local credentials — `.env.example` is the committed template and must survive
# the wildcard below (it would otherwise match `.env.*`).
.env
.env.*
!.env.example
